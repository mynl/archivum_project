[package]
name = "rustfuzz"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.20", features = ["extension-module"] }
fuzzy-matcher = "0.3"

[package.metadata.maturin]
name = "rustfuzz"
